---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionNav from '../components/SectionNav.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<BaseLayout title="SB 54 in California" description="SB 54 interest-holder education materials — understand the law, your role, and how to access reimbursement.">
  <SectionNav currentPath={`${base}/`} />
  <div class="page">
    <!-- Color-blocked hero — adapts the live /california layered-panel device:
         dark panel + reverse white text + photo collage, offset against gold + teal mats. -->
    <div class="hero--blocked">
      <div class="hero-panel">
        <div class="hero-panel__media">
          <figure class="hero-fpo hero-fpo--filled">
            <img src={`${base}/fpo/1.png`} alt="Layered Unity Spark arcs in teal and navy — abstract circular motion." />
          </figure>
          <figure class="hero-fpo hero-fpo--short hero-fpo--filled">
            <img src={`${base}/fpo/2.png`} alt="Clean recovered recyclable materials arranged as valuable commodities." />
          </figure>
        </div>
        <div class="hero-panel__body">
          <h1><em>SB&nbsp;54</em> in California</h1>
          <p>
            California is taking on one of its most ambitious recycling and packaging challenges yet. SB&nbsp;54 is a groundbreaking law that shifts responsibility for funding the recycling system to the companies that make and sell packaging, and sets bold goals for what that system can achieve.
          </p>
          <nav class="hero-links" aria-label="Jump to a section">
            <a href={`${base}/whats-changing/`}>What&rsquo;s Changing &rarr;</a>
            <a href={`${base}/roles-and-responsibilities/`}>Roles &rarr;</a>
            <a href={`${base}/reimbursement/`}>Reimbursement &rarr;</a>
          </nav>
        </div>
      </div>
    </div>

    <!-- Stat band — reproduces the live CAA "Key Goals" grid -->
    <div class="band band--tint-teal">
      <p class="band__eyebrow">By January 1, 2032, SB 54 aims to:</p>
      <div class="checks">
        <div class="check">
          <span class="check__mark" aria-hidden="true">&check;</span>
          <span class="check__text">25% reduction in single-use plastic packaging</span>
        </div>
        <div class="check">
          <span class="check__mark" aria-hidden="true">&check;</span>
          <span class="check__text">65% recycling rate</span>
        </div>
        <div class="check">
          <span class="check__mark" aria-hidden="true">&check;</span>
          <span class="check__text">100% of packaging recyclable or compostable</span>
        </div>
      </div>
    </div>

    <!-- Split: who this is for, beside a brand graphic -->
    <div class="split split--media-right">
      <div class="split__media media-frame">
        <figure class="fpo fpo--filled">
          <img src={`${base}/fpo/3.png`} alt="Layered semi-transparent Unity Spark arcs in teal and navy — a system coming together." />
        </figure>
      </div>
      <div class="split__body">
        <p class="hub__subtitle">
          The people who will make that happen are already out there doing this work. If that includes you, this site is here to help you understand what is changing, where you fit, and how to access the support and funding that comes with it.
        </p>
      </div>
    </div>

    <div class="video-placeholder">
      <div class="video-placeholder__inner">
        <span class="video-placeholder__icon" aria-hidden="true">&#9654;</span>
        <span class="video-placeholder__label">Overview video &mdash; coming soon</span>
      </div>
    </div>

    <h2 class="hub__heading">Start with the part that fits you</h2>
    <div class="card-grid">
      <div class="card">
        <span class="card__number">1</span>
        <h3 class="card__title">What SB 54 Means for You</h3>
        <p class="card__description">
          Understand what is changing, what is not, and why it matters for your organization.
        </p>
        <a href={`${base}/whats-changing/`} class="card__cta sqs-block-button-element sqs-block-button-element--medium sqs-button-element--primary">Start here &rarr;</a>
      </div>

      <div class="card">
        <span class="card__number">2</span>
        <h3 class="card__title">Roles and Responsibilities</h3>
        <p class="card__description">
          See how your work connects to the bigger picture and where you fit in the new system.
        </p>
        <a href={`${base}/roles-and-responsibilities/`} class="card__cta sqs-block-button-element sqs-block-button-element--medium sqs-button-element--primary">Find your role &rarr;</a>
      </div>

      <div class="card">
        <span class="card__number">3</span>
        <h3 class="card__title">Reimbursement for Covered Costs</h3>
        <p class="card__description">
          Producer funding is flowing back into the system. Find out what is eligible and how to prepare.
        </p>
        <a href={`${base}/reimbursement/`} class="card__cta sqs-block-button-element sqs-block-button-element--medium sqs-button-element--primary">See reimbursement &rarr;</a>
      </div>
    </div>

    <div class="hub__links">
      <a href="mailto:internalcomms@circularaction.org">Contact CAA Communications</a>
    </div>
  </div>
</BaseLayout>

<style>
  /* Hero composition lives in global.css (.hero--blocked et al.) so module
     pages can reuse it. This block holds hub-specific bits only. */
  .hub__subtitle {
    font-size: var(--text-lg);
    color: var(--caa-navy);
    max-width: var(--max-line-length);
    opacity: 0.85;
  }

  .hub__heading {
    margin-top: var(--space-2xl);
  }

  .card__number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--caa-teal);
    color: var(--caa-paper);
    font-family: var(--font-display);
    font-size: var(--text-sm);
    font-weight: 700;
    margin-bottom: var(--space-sm);
  }

  .hub__links {
    padding-top: var(--space-2xl);
    font-size: var(--text-sm);
  }

  /* Full-strength accessible teal link (the container's old opacity:0.7 washed it out). */
  .hub__links a {
    color: var(--caa-teal-dark);
    font-weight: 600;
  }
</style>
